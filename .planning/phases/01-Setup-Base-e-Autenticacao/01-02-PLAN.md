---
phase: 01-Setup-Base-e-Autenticacao
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [frontend/src/lib/supabase.ts, frontend/src/App.tsx, frontend/src/pages/Login.tsx, frontend/src/pages/Register.tsx, frontend/src/components/ProtectedRoute.tsx]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "A aplicação possui formulário de cadastro funcional."
    - "A aplicação possui formulário de login funcional."
    - "O estado de autenticação é preservado via contexto e rotas privadas são bloqueadas para usuários não autenticados."
  artifacts:
    - path: "frontend/src/lib/supabase.ts"
      provides: "Instância configurada do cliente Supabase."
    - path: "frontend/src/pages/Login.tsx"
      provides: "Interface de login de usuário."
  key_links:
    - from: "frontend/src/pages/Login.tsx"
      to: "frontend/src/lib/supabase.ts"
      via: "Utiliza supabase.auth.signInWithPassword"
---

<objective>
Implementar a camada de autenticação visual no frontend utilizando o provedor Supabase e preparar rotas públicas e privadas.

Purpose: Permitir que apenas usuários logados acessem a área do Bolão e consigam salvar palpites na fase seguinte.
Output: Telas de Cadastro, Login, e Contexto de Autenticação configurado.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-Setup-Base-e-Autenticacao/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configurar Cliente Supabase</name>
  <files>frontend/src/lib/supabase.ts</files>
  <action>Criar o arquivo do cliente consumindo as variáveis de ambiente (`import.meta.env.VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY`) instanciando o `createClient`.</action>
  <verify>O arquivo é criado sem erros de TS.</verify>
  <acceptance_criteria>
    - `frontend/src/lib/supabase.ts` exporta a instância configurada do Supabase.
  </acceptance_criteria>
  <done>Cliente Supabase exposto para a aplicação.</done>
</task>

<task type="auto">
  <name>Task 2: Criar Contexto de Autenticação (Session)</name>
  <files>frontend/src/contexts/AuthContext.tsx</files>
  <action>Criar um provedor que utiliza `supabase.auth.onAuthStateChange` para ouvir alterações de sessão (login/logout). Armazenar a session em um React state.</action>
  <verify>A compilação do TypeScript para esse arquivo passa sem erros.</verify>
  <acceptance_criteria>
    - O contexto React provê os dados do `user` logado e `session`.
  </acceptance_criteria>
  <done>Contexto escutando mudanças do Supabase Auth.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar Telas de Login e Register</name>
  <files>frontend/src/pages/Login.tsx, frontend/src/pages/Register.tsx, frontend/src/App.tsx</files>
  <action>Criar telas com formulários HTML semânticos integrados aos métodos `supabase.auth.signInWithPassword` e `supabase.auth.signUp`. Estilizar usando TailwindCSS para ficar moderno e visualmente elegante. Criar rotas no `App.tsx` (usando react-router-dom) apontando para essas telas, protegendo as demais.</action>
  <verify>Rotas renderizam e compilam.</verify>
  <acceptance_criteria>
    - As funções de signup e signin estão conectadas aos inputs do formulário.
    - Telas bonitas e funcionais com roteamento no `App.tsx`.
  </acceptance_criteria>
  <done>Login e Cadastro integrados.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] O cliente Supabase deve estar inicializado.
- [ ] Telas de autenticação (formulários) devem estar funcionais no código.
- [ ] Contexto de Auth deve existir para ser consumido pelas próximas fases.
</verification>

<success_criteria>
- Telas de Autenticação e componente de AuthContext finalizados e conectados ao Supabase.
</success_criteria>

<output>
After completion, create `.planning/phases/01-Setup-Base-e-Autenticacao/01-02-SUMMARY.md`
</output>
