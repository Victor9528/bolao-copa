---
phase: 01-Setup-Base-e-Autenticacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, frontend/package.json, backend/package.json, frontend/vite.config.ts]
autonomous: true
requirements: [TECH-02, TECH-03, TECH-04]
user_setup:
  - service: supabase
    why: "O banco de dados e a autenticação exigem credenciais reais de um projeto no Supabase."
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Project Settings -> API -> Project URL"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Project Settings -> API -> Project API Keys -> anon public"
must_haves:
  truths:
    - "Projeto frontend Vite + React + TS inicia sem erros na porta padrão."
    - "Projeto backend Node.js + Express inicia sem erros."
    - "Variáveis de ambiente do Supabase estão configuradas."
  artifacts:
    - path: "frontend/package.json"
      provides: "Dependências do React"
    - path: "backend/package.json"
      provides: "Dependências do Node"
  key_links: []
---

<objective>
Configurar o ambiente de desenvolvimento inicial, separando claramente o Frontend (Vite) do Backend (Node.js) e instalar o SDK do Supabase.

Purpose: Ter a infraestrutura limpa e estruturada, pronta para receber a lógica de autenticação.
Output: Pastas `frontend` e `backend` criadas e inicializadas.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup do repositório root e workspace</name>
  <files>package.json</files>
  <action>Criar um package.json na raiz para orquestrar os scripts se necessário, porém o foco será criar as pastas `frontend` e `backend`. Criar `frontend/.env.example` com as chaves `VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY`.</action>
  <verify>ls frontend/.env.example retorna sucesso.</verify>
  <acceptance_criteria>
    - `frontend/.env.example` contém chaves do Supabase.
  </acceptance_criteria>
  <done>Variáveis do Supabase prontas para preenchimento humano.</done>
</task>

<task type="auto">
  <name>Task 2: Inicializar o Frontend Vite + React</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/src/main.tsx</files>
  <action>Rodar `npm create vite@latest frontend -- --template react-ts`. Instalar Tailwind CSS, postcss e autoprefixer. Instalar `@supabase/supabase-js`. Configurar o `tailwind.config.js` e diretivas no `index.css`.</action>
  <verify>A pasta `frontend/node_modules` é criada ou `frontend/package.json` possui dependências.</verify>
  <acceptance_criteria>
    - Vite e Tailwind estão configurados no `package.json` do frontend.
    - O pacote `@supabase/supabase-js` está incluído.
  </acceptance_criteria>
  <done>Frontend scaffolding criado com sucesso.</done>
</task>

<task type="auto">
  <name>Task 3: Inicializar o Backend Node.js + Express</name>
  <files>backend/package.json, backend/tsconfig.json, backend/src/index.ts</files>
  <action>Na pasta `backend`, criar um projeto npm (`npm init -y`), instalar `express`, `cors`, `dotenv` e typescript/ts-node. Criar um servidor básico `index.ts` respondendo com um 'Hello Bolão API' na porta 4000.</action>
  <verify>Backend compilável e `package.json` existente.</verify>
  <acceptance_criteria>
    - `backend/src/index.ts` exporta e inicializa o express.
  </acceptance_criteria>
  <done>Backend Node.js configurado e funcional.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] As pastas frontend e backend devem existir.
- [ ] O `package.json` do frontend deve listar dependências do Vite, Tailwind e Supabase.
- [ ] O `.env.example` do frontend deve ter os placeholders do Supabase.
</verification>

<success_criteria>
- Infraestrutura separada entre frontend e backend com êxito.
- Projeto pronto para uso do Supabase.
</success_criteria>

<output>
After completion, create `.planning/phases/01-Setup-Base-e-Autenticacao/01-01-SUMMARY.md`
</output>
